{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build:prod": "mkdir -p dist && bun build src/index.ts --production --outfile dist/index.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test"
  },
  "dependencies": {
    "@cssg/shared": "workspace:*",
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3"
  }
}
