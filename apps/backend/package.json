{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@cssg/shared": "workspace:*",
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3"
  }
}
